#include<iostream>
using namespace std;
#include<cmath>
/*int main(){
    //simple interest
    int P,R,T,SI;
    cout<<"enter principal";
    cin>>P;
    cout<<"enter rate";
    cin>>R;
    cout<<"enter time";
    cin>>T;
    SI= (P*R*T)/100;
    cout<<SI;
    return 0;
}*/
int main(){
    /*float b,h,AOT;
    cout<<"enter base";
    cin>>b;
    cout<<"enter hieght";
    cin>>h;
    AOT= 1/2.0 *(b*h);
    cout<< "Area of triangle is" <<AOT;*/

    //cout<<7/2;
    //cout<<7/2.0;
    //cout<<7%2;

    //compound interest
    /*
    int P,R,T,n;
    cout<<"enter principal";
    cin>> P;
    cout<<"enter rate";
    cin>> R;
    cout<<"enter no. years";
    cin>> T;
    cout<<"enter n";
    cin>>n;
    cout<<"compound interest is"<<P*pow((1+R/n),n*T);
    return 0;
    */

   //roots of quadratic equation
   //try this with such an input that roots come out to be real
   /*int a,b,c;
   cout<<"general quafratic equation is ax^2 + bx +c " ;
   cout<<"enter a";
   cin>> a;
   cout<<"enter b";
   cin>> b;
   cout<<"enter c";
   cin>> c;
   cout<<"roots of equation are "<< (-b + sqrt(pow(b,2) -4*a*c))/2*a , (-b -sqrt((pow(b,2) -4*a*c))/2*a);
   */

  //area of rectangle
  /*int l,b,a;
  cout<<"enter length ";
  cin>>l;
  cout<<"enter breadth ";
  cin>>b;
  a=l*b;
  cout<<"area of rectangle is "<<a;
  */

  //area of circle
  /*
  float r,a;
  cout<<"enter radius";
  cin>>r;
  a=3.14*3.14*r;
  cout<<"area of circle is"<<a;
  */
  
  //to check if a number is even or odd
  /*int n;
  cout<<"enter number";
  cin>>n;
  if (n%2==0){
      cout<<n<<" is even";
  }
  else{
      cout<<n<<" is odd";
  }*/
  
  //to check bigger of 2 unequal numbers
  /*int a,b;
  cout<<"enter a";
  cin>>a;
  cout<<"enter b";
  cin>>b;
  if (a>b)
  {
      cout<<a<<" is bigger";
  }
  else if (b>a)
  {
      cout<<b<<" is bigger";
  }
  else
  {
      cout<<"the numbers are equal";
  }
  
  */
  //biggest of 3 unequal numbers
  /*int a,b,c;
  cout<<"enter a";
  cin>>a;
  cout<<"enter b";
  cin>>b;
  cout<<"enter c";
  
  cin>>c;
  if (a>b && a>c)
  {
      cout<<a<<" is biggest";
  }
   else if (b>a && b>c) 
   {
       cout<<b<<" is biggest";
   }
   else 
   { 
       cout<<c<<" is biggest";
   }   
   */
    
  return 0;
}
// 