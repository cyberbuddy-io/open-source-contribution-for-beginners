import java.util.*;
public class HashSett {
    public static void main(String[] args) {
        // hashset class
        // hashmap class
        // linked hashmap
        // hashtable class
        HashSet <Integer> h=new HashSet<>();
        h.add(6);
        h.add(8);
        h.add(5);
        h.add(5);//only one  5 will be stored no duplicate value is stored hashset
        System.out.println(h);
    }
}
